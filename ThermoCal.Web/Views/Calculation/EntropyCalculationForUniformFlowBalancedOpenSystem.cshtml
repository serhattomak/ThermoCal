<main id="main" class="main">
    <div class="pagetitle">
        <h1>Hesaplama</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Anasayfa</a></li>
                <li class="breadcrumb-item">Hesaplama</li>
                <li class="breadcrumb-item active">Uniform Akışlı Dengeli Açık Sistemler için Entropi Hesaplama</li>
            </ol>
        </nav>
    </div><!-- End Page Title -->
    <section class="section">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Uniform Akışlı Dengeli Açık Sistemler için Entropi Hesaplama</h5>

                        <!-- Floating Labels Form -->
                        <form class="row g-3" id="calculationForm">
                            <!-- Fields Specific to EntropyCalculationForUniformFlowBalancedOpenSystemRequestDto -->
                            <div class="col-md-6">
                                <div class="form-floating">
                                    <input type="text" class="form-control" id="mdotIn" name="MDotIn" placeholder="Kütlesel Debi Giriş (ṁ,in)">
                                    <label for="mdotIn">Kütlesel Debi Giriş (ṁ,in)</label>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-floating">
                                    <input type="text" class="form-control" id="mdotOut" name="MDotOut" placeholder="Kütlesel Debi Çıkış (ṁ,out)">
                                    <label for="mdotOut">Kütlesel Debi Çıkış (ṁ,out)</label>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-floating">
                                    <input type="text" class="form-control" id="qr" name="Qr" placeholder="Isı (Qr)">
                                    <label for="qr">Isı (Qr)</label>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-floating">
                                    <input type="text" class="form-control" id="tr" name="Tr" placeholder="Sıcaklık (Tr)">
                                    <label for="tr">Sıcaklık (Tr)</label>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-floating">
                                    <input type="text" class="form-control" id="sIn" name="SIn" placeholder="Entropi (S,giren)">
                                    <label for="sIn">Entropi (S,giren)</label>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-floating">
                                    <input type="text" class="form-control" id="sOut" name="SOut" placeholder="Entropi (S,çıkan)">
                                    <label for="sOut">Entropi (S,çıkan)</label>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-floating">
                                    <input type="text" class="form-control" id="sCVFirst" name="SCVFirst" placeholder="Entropi (S_CV1)">
                                    <label for="sCVFirst">Entropi (S_CV1)</label>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-floating">
                                    <input type="text" class="form-control" id="sCVSecond" name="SCVSecond" placeholder="Entropi (S_CV2)">
                                    <label for="sCVSecond">Entropi (S_CV2)</label>
                                </div>
                            </div>

                            <div class="text-center">
                                <button type="button" class="btn btn-primary" id="calculate">Hesapla</button>
                                <button type="reset" class="btn btn-secondary">Sıfırla</button>
                            </div>
                        </form><!-- End floating Labels Form -->

                        <div class="mt-3">
                            <label for="entropyResult" class="form-label">Sonuç Entropi Jenerasyonu (SGen)</label>
                            <input type="text" class="form-control" id="entropyResult" readonly>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>

<script type="module">
    document.getElementById("calculate").addEventListener("click", async function () {
        var formData = {
            MDotIn: parseFloat(document.getElementById("mdotIn").value),
            MDotOut: parseFloat(document.getElementById("mdotOut").value),
            Qr: parseFloat(document.getElementById("qr").value),
            Tr: parseFloat(document.getElementById("tr").value),
            SIn: parseFloat(document.getElementById("sIn").value),
            SOut: parseFloat(document.getElementById("sOut").value),
            SCVFirst: parseFloat(document.getElementById("sCVFirst").value),
            SCVSecond: parseFloat(document.getElementById("sCVSecond").value)
        };

        console.log("Form Data:", formData);

        try {
            const response = await fetch('/Calculation/EntropyCalculationForUniformFlowBalancedOpenSystem', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(formData)
            });

            const responseText = await response.text();
            console.log("Raw API Response:", responseText);

            if (!response.ok) {
                console.error('API returned error:', responseText);
                throw new Error('API returned error: ' + responseText);
            }

            let result;
            try {
                result = JSON.parse(responseText);
            } catch (e) {
                console.error('Failed to parse JSON response:', responseText);
                throw new Error('Failed to parse JSON response: ' + responseText);
            }

            console.log("Parsed API Response:", result);

            if (result && result.data && result.data.sGen !== undefined) {
                document.getElementById("entropyResult").value = result.data.sGen;
            } else {
                console.error('API returned unexpected response:', result);
                alert('Error: API returned unexpected response');
            }
        } catch (error) {
            console.error('An error occurred:', error);
        }
    });
</script>
